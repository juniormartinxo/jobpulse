global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "jobpulse-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

  - job_name: "jobpulse-worker"
    static_configs:
      - targets: ["worker:8002"]
